## 🐳 Infrastructure

- Include Docker setup ONLY for PostgreSQL
- Provide a docker-compose.yml with:
  - Postgres 15
  - Exposed port 5432
  - Volume for persistence

- Do NOT dockerize Next.js (runs locally)
